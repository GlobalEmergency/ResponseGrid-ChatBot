{
  "name": "responsegrid-chatbot",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "Multichannel Telegram + WhatsApp assistant (OpenAI Agents SDK) for the ResponseGrid humanitarian-aid API.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "mcp": "tsx watch src/mcp-server.ts",
    "mcp:start": "node dist/mcp-server.js",
    "test": "tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@openai/agents": "latest",
    "dotenv": "latest",
    "openai": "latest",
    "telegraf": "latest",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=24"
  }
}
